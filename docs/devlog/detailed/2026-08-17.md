# Devlog — 2026-08-17

## The red audit gate was three dev-only advisories, and `npm audit fix` would not fix them

**Timestamp:** 2026-08-17 21:10–21:45

**Action:** `npm update brace-expansion nanoid postcss --package-lock-only`, then a full install and all gates. Committed as `7c2e8e0`; the lesson landed as `3257698`.

**Result:** `npm audit --audit-level=high` full-tree went 3 high → 0; `--omit=dev` was already 0. Lockfile diff was 13 insertions / 12 deletions; `package.json` untouched; production worker byte-identical at 148,916 / 150,000.

**Reasoning:** The obvious tool was `npm audit fix`, and it reported "fix available" while leaving the lockfile byte-identical and printing "up to date" — a no-op indistinguishable from success without diffing the lockfile. `npm update <pkg> --package-lock-only` is what re-resolved the transitive pins. The full trap is lesson 27 in `docs/learning/lessons.md`.

**Validation:** 718 tests / 117 files, typecheck, zero-warning lint, production build, both audit trees at 0. The byte-identical worker is the proof nothing shipped changed. What the proof would miss: the three sibling-linked copies of the same packages inside `../civ-engine` and `../voxel` node_modules are those repos' own lockfiles to fix — city's audit reads city's tree.

**Code reviewer comments:** None ran. The canon's independent-critic and multi-cli-review escalations were skipped, stated explicitly in-session, because this session is configured not to spawn agents unprompted; judged proportionate for a dev-only transitive patch bump with byte-identical output.

**Notes:** The same lockfile write absorbed the `../voxel` link metadata 0.1.4 → 1.2.0 — npm re-reads linked siblings' current `package.json` on any lockfile touch, so a lockfile change can carry sibling drift that was not authored here. A live session was moving voxel concurrently (its HEAD advanced mid-session); worktree and evidence cleanup were verified against that before touching anything.

## D1: the trip system obeys one clock, and rush hour appears on its own

**Timestamp:** 2026-08-17 22:10–23:55

**Action:** `src/protocol/city-clock.ts` (TICKS_PER_DAY and DAY_START_FRACTION moved here; windows night/morning/day/evening as integer local-tick boundaries; `cityClock`/`clockTime` shared by sim, renderer lighting, and HUD), `src/sim/routine.ts` (identity-hashed departure offsets via the exported `identityWord`, zero world-RNG draws; `workDepartureAt`/`homeDepartureAt`/`outingAllowed`/`restUntilTick`), and the rewiring: the trip system clock-gates every leg at its single decision point, arrivals park schedule dwells instead of random cooldowns (`WORK_WAIT_*`, `HOME_COOLDOWN_*`, `REST_*` deleted), night converts a planned outing to rest while morning parks it for the day window.

**Result:** Emergent double hump with nobody scripting it: seed-3 organic quarter-scale city measures morning peak 23, evening peak 27, night mean 0.0, day mean 0.01 active vehicles. Fixture-city projection at tick 1203 honestly moved 81 cars / 17 walkers → 77 / 2 (tick 1203 is now early evening — the homeward rush with outings not yet begun). 736 tests / 120 files, typecheck, zero-warning lint; production worker 150,329 / 158,000 after a conscious budget raise documented in the check script's own history.

**Reasoning:** The sim was clock-blind — only the renderer and day counter read `TICKS_PER_DAY`, and trips paced by cooldown, so 3 AM equaled 8 AM. Anchoring the loop rather than reweighting trip mix is the user's direction (routines first, patterns emergent). The signal-phase precedent decided where the clock lives: protocol, because the renderer may import protocol but never sim, and folding the app-side `DAY_START_FRACTION` into the shared function makes "looks like morning" and "behaves like morning" one fact.

**Validation:** Mutation check before trusting the new contracts — cooldown-style behavior restored temporarily fails 4 of the scenario contracts, restored code passes all 9; the emergent margins were then measured, not assumed. Browser: fixture city paused and stepped deterministically; 02:29 reads 0 vehicles / 0 pedestrians with night lighting, 07:20 reads 34 + 28 with sunrise light, HUD renders "Day 1 · 07:20" from the same function. What the proof would miss: nothing exercises D1 against a *live player* mid-window save/load beyond the converges-after-save/load test.

**Code reviewer comments:** None ran (session configured not to spawn agents unprompted). Skipped escalations stated in-session; D2's save-format change is pre-flagged for multi-cli-review.

**Notes:** Three findings a later session should know. (1) The performance fixture city is growth-unstable over long live runs: frozen mid-growth at tick 1202, run live it triples its buildings past its pinned plant/pump and collapses (972 → 9 people in ~3,700 ticks) — fine for its 1-tick benchmark purpose, wrong as a long-horizon visual scene; the D1 collapse observed in-browser was this, not a routine regression (growth/demand/employment read no trip state). (2) The scenario-test reckoning has one recurring shape: outing/rest intents seeded at boot now meet the clock (boot is mid-morning), so tests advance to the honest window first via the new `stepToWindow` helper; the invalid-shop rejection test switched to driving `chooseOutingDestination` directly because waiting for the day window let growth heal the seeded defect (an abandoned shop recovers in ~48 ticks) — live-stepping raced recovery instead of testing rejection. (3) Midday streets measure ~0 vehicles by honest consequence of the household-scale model: every household's representative is at work, and nobody else exists to run errands. That emptiness is the measurable gap D2's simultaneous member lives close.

## D2: a second person in the household got their own life, and the save format survived it

**Timestamp:** 2026-08-18 00:10–02:05

**Action:** `memberTrip` component (per-member slots: toPlace/atPlace/toHome with generation-guarded place; slot absence = home) registered append-last per the citizenProfile precedent — no saveVersion bump; `src/sim/traffic/schools.ts` (school choice by the nearestVenues ranking within the school radius, slot mutation, walker arrival/cancel consequences); the member pass in trips.ts with its own cursor and the shared walker budget; `school` pedestrian purpose through the projection, purpose counters, and a wardrobe palette (which was in fact unreachable dead code until the 2026-08-18 review — see that entry); per-member whereabouts in citizen-detail and the panel roster line.

**Result:** 745 tests / 121 files; worker 154,196 / 158,000. The headline contract holds: worker `atWork` while the child's slot reads `atPlace` at the school, simultaneously, with both walkers having coexisted. Save/load mid-walk continues byte-identically for 600 ticks; a stripped legacy snapshot loads as everyone-home and fabricates nothing.

**Reasoning:** School runs need simultaneity, not just a destination — a child cannot displace the worker's commute from the household's single trip slot, which is why D1 explicitly deferred them. Slots live in a separate rare-write component so hot tick diffs stay small, and the schools module owns no spawning so pedestrians → schools stays a one-way import.

**Validation:** Mutation check: disabling the member pass fails 4 of 9 contracts; restored, all pass. Browser: the raw-command build-a-town path kept dying to real gameplay hazards (validate-at-submit raced same-batch roads, growth waited on utilities, a mis-networked power line), so the proof town was generated headlessly under the exact shipping worker flags and injected as a save — the child was then photographed mid-walk on the school street at 08:28 with `pedestrianPurposes.school: 1`. What the proof would miss: multi-child households at scale (one staged child covered), and the multi-cli adversarial review of the persistence change is still owed — recorded as the next unit rather than silently skipped.

**Notes:** Traps a later session should know. (1) Raw `worker.postMessage` commands validate at submit against the pre-drain world — a zone sent in the same batch as its road fails `nearRoad`; the client's `submit()` path hides this because the UI validates against the already-updated mirror. (2) The browser fs-shim applies inside vitest too — `node:fs` is aliased away, so a dev-save generator hands its output through stdout markers instead. (3) `windowStart(tick, w)` returns the start of the *current* occurrence when inside `w` — school dismissal uses its own next-occurrence arithmetic for the 15:00 bell.
