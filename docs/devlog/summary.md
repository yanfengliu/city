# Devlog Summary

History before 2026-08-17 lives in `PROGRESS.md` § Log — the phase-by-phase record this devlog supersedes going forward.

- 2026-08-19: **Foliage that floated off its trunk** — reported as "tree body is separated from trunk", and it was: `lift` positioned each canopy layer by its NOMINAL height box, but a `DodecahedronGeometry` fills only 93.4% of that box vertically, so the broadleaf's canopy floor sat 0.100 above its trunk top and the columnar's 0.076 — both hovering. `createCanopyGeometry` now seats a layer by its computed lowest vertex, and a new per-archetype `canopySink` settles the whole foliage stack onto the trunk (conifer 0.05 / broadleaf 0.15 / columnar 0.16). **The instrument was the interesting part:** the first version of the contract test measured the WIDEST cross-section at the joint and passed a canopy that still showed daylight — a faceted bottom is an edge, so it reads 0.158 wide from one azimuth and 0.040 from the perpendicular one, and the trees are randomly rotated. Measuring the section's narrowest side instead is what set the shipped sink values. Narrowest section at the trunk top went 0.000 → 0.318 / 0.141 / 0.085 against trunk radii 0.055 / 0.075 / 0.060. Landed as lesson 29. Before/after screenshots at an identical camera; whole-scene sweep clean (park trees are a separate, already-correct code path). 764 tests; worker unchanged at 157,158 / 158,000.

- 2026-08-18 (last): **Routines feed back: D3 lands and the daily-routines plan is complete** — a school arrival credits `completedSchoolTrips` and stamps the HOME BUILDING's `schoolingTick` (stamped on the building, not the household, so the level gate needs no per-tick resident lookup; childless homes stamped by the same morning scan, so the gate reads one uniform field). Education now needs coverage AND a current stamp, which closes the loop on the D2 review's finding 3: a coverage overlay knows nothing about roads, so a school across an unbridged river used to level buildings it could never teach. Groceries join happiness proven by arrival, never proximity. `routineGap` is kept deliberately separate from `strandedAt` — broken road vs. never-built destination are different problems with different fixes, so they carry different text, weights, and advisories. Browser-verified: no school → `homesWithoutSchool: 1`, 0 trips; place one → gap 0, 6 real arrivals. 763 tests; worker 157,158 / 158,000 — **842 bytes left, the next feature here needs the budget raised.**

- 2026-08-18 (later): **Adversarial review of D2 finds seven real defects, including one that poisoned saves** — two independent CLI lenses over `3363ae7..c306b1f`, both probe-verified. Fixed, each with a contract test seen RED for the right reason first: (1) `unassignWorkers` destroyed a child's school walker raw, stranding her `memberTrip` slot FOREVER on any workplace bulldoze or abandonment — she never attended again and the zombie slot serialized into every later save, deterministically, so the replay gate stayed green while the state was wrong; (2) `activeAgent` bound the household panel to the child's walker, reporting a driving commuter as "home"; (3) `schoolFor` re-derived the service radius as Manhattan while the overlay and growth's `educated` use Chebyshev, so diagonal homes read covered but never attended — now consumes the coverage field itself, and filters to the home's road component; (4) a post-bell arrival booked TOMORROW's bell, holding the child at school ~27 hours; (5) household outings could depart carrying a member already out at school — the same person rendered twice; (6) the school wardrobe palette was dead code while PROGRESS and this devlog both claimed it rendered; (7) school runs took all 256 walker slots, measured at 8% of morning ticks on a 494-household town, now 0% with a reserved share. Plus `memberTrip` load sanitizing. **The lesson that cost the most:** finding 3's first test PASSED with the fix deliberately disabled — an unreachable school produces no walker, which is indistinguishable from "not morning yet". Landed as lesson 28. 757 tests; worker 155,238 / 158,000.

- 2026-08-18: **Children walk to school while their parents work: D2 member lives land** — the appended `memberTrip` component gives non-primary members their own generation-guarded slots; a child departs in the morning window by its own identity-hashed moment, dwells to the 15:00 bell, walks home; placing a school is what creates school runs. 9 contracts in `school-runs.test.ts` (simultaneity, windows, save round-trip mid-walk, legacy-safe, deterministic; mutation-checked), per-member whereabouts in the citizen detail and panel roster, `school` purpose (its wardrobe palette was claimed here but dead until the 2026-08-18 review wired it up). Browser-verified via a headlessly generated living save: the child photographed mid-walk on the school street at 08:28 with `pedestrianPurposes.school: 1`. 745 tests; worker 154,196 / 158,000.

- 2026-08-17 (later): **The city lives by its clock: D1 rush hour emerges** — commutes now depart in the morning window and return in the evening on one shared `cityClock`, and the traffic pattern follows with nobody scripting it: seed-3 organic city measures morning peak 23 / evening peak 27 / night mean 0.0 vehicles (`rush-hour.test.ts`), photographed live at 02:29 (empty dark streets), 07:20 (34 cars + 28 walkers), 17:23 (evening rush). 736 tests; worker budget consciously 150k → 158k; no save change. Midday is honestly dead — every household is at work — which is D2's job to fix with member lives.

- 2026-08-17: Nothing behaviour-changing, but the next milestone is set: **v1.1 Living Days** — user direction that rush hour and school runs must emerge from actual people living clock-anchored daily routines, never from scripted traffic curves; phased as D1–D3 in `simulation-realism.md` § Daily routines. Also cleared three high dev-tree advisories (`brace-expansion`/`nanoid`/`postcss`, lockfile-only, production worker byte-identical at 148,916), landed the `npm audit fix` no-op trap as lesson 27, synced the fleet canon (independent critic, instrument verification, whole-frame sweep), removed two stale clean worktrees, and created this devlog.
